// Write code to create a function that accepts an array of integers sorted in ascending (increasing) order and returns a new array containing the squares of each number in ascending ord


// Alternate solution (less efficient)

// var sortedSquares = function(arr) {
//   var squares = arr.map(function(num) {
//     return num ** 2;
//   });

//   var sortedSquares = squares.sort(function(a, b) {
//     return a - b;
//   });

//   return sortedSquares;
// };
